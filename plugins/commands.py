# All the commands that can be called and their configurations

hello = {"pattern": "\.hello", "incoming": True, "outgoing": True, }
ping = {"pattern": "\.ping", "incoming": True, "outgoing": True, }
speed = {"pattern": "\.speed", "incoming": True, "outgoing": True, }
insult = {"pattern": "\.insult", "incoming": True, "outgoing": True, }
ytv = {"pattern": "\.ytv", "incoming": True, "outgoing": True, }
id = {"pattern": "\.id", "incoming": True, "outgoing": True, }
yta = {"pattern": "\.yta", "incoming": True, "outgoing": True, }
red = {"pattern": "\.red", "incoming": True, "outgoing": True, }
