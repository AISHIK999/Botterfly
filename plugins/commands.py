# All the commands that can be called and their configurations

# A command should be processed after the trigger
# Change the trigger as per need
trigger = "$"

hello = {
    "pattern": f"(?i)\\{trigger}hello",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
ping = {
    "pattern": f"(?i)\\{trigger}ping",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
speed = {
    "pattern": f"(?i)\\{trigger}speed",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
ytv = {
    "pattern": f"(?i)\\{trigger}ytv",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
id = {
    "pattern": f"(?i)\\{trigger}id",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
yta = {
    "pattern": f"(?i)\\{trigger}yta",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
help = {
    "pattern": f"(?i)\\{trigger}help",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
reddit = {
    "pattern": f"(?i)\\{trigger}reddit",
    "incoming": True,
    "outgoing": True,
    "from_users": "me",
}
